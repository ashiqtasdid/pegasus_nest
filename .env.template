# Environment Template for Pegasus Nest API
# Copy these values to your .env file or set in your environment

# VPS_SSH_KEY - Your private SSH key to access the VPS (for deployment only)
# Generate with: ssh-keygen -t rsa -b 4096 -C "github-actions"
# Add the public key to your VPS: ~/.ssh/authorized_keys

# API Environment Variables
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Optional Configuration
NODE_ENV=production
PORT=3000

# API Security (recommended for production)
API_SECRET_KEY=your_api_secret_key_here_32_chars_min
